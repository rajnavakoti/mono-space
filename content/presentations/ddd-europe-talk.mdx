---
title: "Reverse-Engineering DDD: Discovering Domains in Legacy Systems Without the Textbook"
event: "DDD Europe 2026"
date: "2026-06-09"
description: "Forensic DDD: treating a legacy system like an archaeological site and using production artefacts to reverse-engineer the domain model that nobody ever documented."
---

<TitleCard badge="DDD EUROPE 2026 — ANTWERP" subtitle="Discovering Domains in Legacy Systems Without the Textbook">Reverse-Engineering DDD</TitleCard>

<Notes>
Title slide. Let it sit for a moment. Don't rush. Take a breath.
</Notes>

---

<SpeakerCard name="Raj Navakoti" photo="/profile pic.png" role="Staff Software Engineer" interests="Domain-Driven Design, Enterprise Architecture, AI Systems" link1="WEB — rajnavakoti.com" link2="PAPER — arxiv.org/abs/2603.14057" link3="TOOLKIT — github.com/rajnavakoti/ddd-archaeology" qrUrl="https://rajnavakoti.com" />

<Notes>
"I'm Raj. I work in a large delivery domain — logistics, carriers, warehouses, the full complexity of getting things from A to B at enterprise scale. I've spent the past few years navigating domain complexity in systems where the original architects are long gone. Today I want to share what I've found."
</Notes>

---

<TitleCard badge="ACT 1" subtitle="A corroded lump of bronze">The Artifact</TitleCard>

<Notes>
Transition to Act 1. Pause before the next slide.
</Notes>

---

## The Antikythera Mechanism

<Split ratio="1:1">
<div>

In 1900, divers pulled a **corroded lump of bronze** from a Greek shipwreck.

It sat in a museum for **two years**. Nobody knew what it was.

Then someone looked inside — and found **precision gear wheels**, a millimetre apart, encoding **2,000 years of astronomical knowledge**.

The builders were gone. The documentation was gone. But **the machine remembered everything.**

</div>
<div>

<SlideImage src="/antikythera.webp" alt="The Antikythera Mechanism — the world's first known computer" />

</div>
</Split>

<Notes>
"In 1900, archaeologists pulled a corroded lump of bronze from a Greek shipwreck. It looked like junk. It turned out to be a 2,000-year-old computer — and the user guide was hidden inside the bronze the whole time. Researchers spent 120 years reading it — X-rays in the 70s, 3D imaging in the 2000s. Each new technique revealed another layer of what the builders encoded."
</Notes>

---

<Callout>
Legacy systems are the same. They look like technical debt. They're actually dense archives of domain knowledge — written under deadline pressure, tested by millions of real transactions, validated over decades.

**The architects left. The documentation went stale. But the system kept running. And it remembers everything.**
</Callout>

<Notes>
"Legacy systems are the Antikythera mechanism of our industry. They look like corroded bronze — technical debt, legacy code nobody wants to touch. But inside, encoded in the schemas, the contracts, the error codes, the git history — is everything the original builders knew about the domain. This talk is about how to read what the system has been trying to tell you."
</Notes>

---

## What is "Legacy"?

<Callout>
Legacy isn't about age.

**It's about lost knowledge.**

A system is legacy when the people who understood it are no longer available to explain it.
</Callout>

<Notes>
"Before we go further — let me define legacy. I don't mean old code. A 6-year-old microservice can be legacy if the team that built it has turned over. A 30-year-old monolith might not be legacy if the original team still maintains it. Legacy is about KNOWLEDGE DECAY — when the understanding of why the system works the way it does has left the building."
</Notes>

---

## Every system has a domain model

<Split ratio="1:1">
<div>

> *"A domain model is not a particular diagram. It is the idea that the diagram is intended to convey."*
>
> — **Eric Evans**

Most domain models were **never designed on a whiteboard**.

They were built into the code, the schemas, the contracts — **decision by decision, over years**.

The model is there. It was just never written down.

</div>
<div>

<SlideImage src="/evans-model.webp" alt="Eric Evans on domain models" />

</div>
</Split>

<Notes>
"Eric Evans himself says it — the model is the idea, not the diagram. In most legacy systems, nobody drew the diagram. But the model exists. It's encoded in every API contract, every database table, every error code. Developers embedded domain knowledge into the system without realizing they were building a model. Our job is to read it."
</Notes>

---

## My story

<Split ratio="1:1">
<div>

A data architect was trying to build **canonical data models** for a large delivery domain.

**Top-down, from domain knowledge.** The right approach — but the domain was too large. It was taking months.

I had a question:

> *"The API specs already exist. The contracts are already written. Why start from scratch?"*

</div>
<div>

We took the spec files. Laid them side by side. Started extracting: **data concepts, entities, value objects, relationships.**

In **weeks**, not months.

Then she left the company. The work died. Because it was **manual**.

</div>
</Split>

<Notes>
"Here's how this started for me. I was working with a data architect in a large delivery domain. She was trying to build canonical data models — top-down, the textbook way. But the domain was enormous. Dozens of teams, vendor integrations, legacy platforms. After months, the models were still incomplete. So I asked: the APIs already exist. Why aren't we reading what's already there? We tried it. In weeks we had a working draft model. Then she left the company, and the manual work died with her."
</Notes>

---

## Meanwhile...

<Split ratio="1:1">
<div>

A technology architect did **strategic DDD** — the textbook way.

Event Storming. Domain storytelling. Stakeholder interviews.

**It worked.** But it took multiple sessions, merged outputs, and over **2 years** to produce a current and target state architecture.

</div>
<div>

And the moment the architecture was documented...

**...the system kept evolving.**

The gap between what people remembered and what the system actually does started growing immediately.

</div>
</Split>

<Notes>
"Meanwhile, I watched a technology architect do strategic DDD properly. Event Storming, domain storytelling, the full textbook approach. It worked — beautifully. But in a domain this large, it took multiple sessions, dozens of people, and over two years. And the moment the architecture was documented, the system kept evolving. The documentation started drifting from reality on day one."
</Notes>

---

## The question that started this talk

<Callout>
What if you could **extract a draft domain model** from the artifacts the system already produces — **before** you run your first Event Storming session?

Not to replace the workshop. To **give it a head start.**

To ask *"is this how it works?"* instead of *"how does this work?"*
</Callout>

<Notes>
"That experience left me with a question: what if you could combine both approaches? Start with what the system already knows — the artifacts — then bring the experts in to validate and extend? Not replace Event Storming. Give it a head start. Arrive with evidence instead of blank sticky notes. Ask 'is this how it works?' instead of 'how does this work?' — because people are MUCH faster at reacting to evidence than creating from scratch."
</Notes>

---

## A domain I know well

<Split ratio="1:1">
<div>

**A delivery domain. 30 years in operation.**

<ContentCard title="4 LAYERS" accent="amber" items="Core services — 6 years of modern microservices, Neighboring domains — 30-year-old order management monolith, Vendor platforms — operational backbone for 25+ years, External carriers — automated APIs to Excel spreadsheets" />

</div>
<div>

Nobody designed this landscape holistically.

**It grew.** Vendors came and went. Systems were added. Contracts ended. Markets have different processes.

Some teams have real-time telemetry. Others operate on **phone calls and spreadsheets**.

</div>
</Split>

<Notes>
"Let me show you the domain I'll use as our case study. A delivery domain I know well. 30 years in operation. Four layers: core services built in the last 6 years — modern, microservices, Kubernetes. Neighboring domains running on order management systems that predate most of this room. Vendor platforms that have been the operational backbone for 25 years. And external carriers ranging from fully automated APIs to suppliers who still send data in Excel. Nobody designed this holistically. It grew."
</Notes>

---

## Where the knowledge lives

<Split ratio="1:1">
<div>

<ContentCard title="KNOWLEDGE SCATTERED ACROSS" accent="red" items="Event Storming output from 2 years ago — already partially wrong, Confluence page last updated in 2022 — everyone still links to it, One ops person who understands the carrier integration — leaving next month, Slack threads from 2019 that explain why certain rules exist, Error codes nobody documented but everyone works around" />

</div>
<div>

> *"We don't see what we don't intend to see."*
>
> — **Dave Snowden**, DDD Europe 2018

This isn't a dysfunctional team. This is **every large enterprise delivery domain**.

</div>
</Split>

<Notes>
"And here's where the knowledge about this domain actually lives. An Event Storming output from 2 years ago — already partially wrong because half the team turned over. A Confluence page from 2022 that everyone still links to. One ops person who understands the carrier integration — she's leaving next month. Slack threads from 2019 that nobody can find. Dave Snowden said it at this conference: we don't see what we don't intend to see. Even Event Storming can only capture what the room remembers. But the system? The system remembers everything."
</Notes>

---

<Callout>
**The system remembers everything. We're the ones who forgot.**

Eight forensic techniques. Eight lenses on the same system. Each reveals something the others can't.

Together, they produce a **draft domain model** — extracted from artifacts, not from memory.
</Callout>

<Notes>
"So here's what I'm going to show you. Eight forensic techniques — each one reads a different artifact the system produces. API contracts. Database schemas. Transaction logs. Production logs. Incidents. Data lineage. Error codes. Git history. Each reveals something the others can't see. Together, they produce a draft model that you can take into your next Event Storming session — or use to validate the one you already have. I'm calling it Forensic DDD. Let's start."
</Notes>

---

<TitleCard badge="ACT 2" subtitle="Eight lenses. One emerging model.">The Investigation</TitleCard>

<Notes>
Pause. Act 2 begins. The investigation. From here, every exhibit adds to the emerging model.
</Notes>

---

## The hypothesis starts empty

<ContextMap version="0" />

> The 6 services we know about. 3 black boxes we don't. No lines, no labels, no opinions yet. Every exhibit adds evidence.

<Notes>
"Before we open any artifact — here's what we have. The 6 services we've heard mentioned by name. 3 black boxes we know exist but nothing's documented about. No lines between them. No verdicts. No colours. This is our baseline. Every exhibit from here adds evidence to this map."
</Notes>

---

<TitleCard badge="EXHIBIT A" subtitle="Teams are polite in workshops. Contracts are honest.">Contract Archaeology</TitleCard>

<Notes>
"First lens. We read the API contracts — what teams declared publicly. 6 OpenAPI specs, 2 AsyncAPI event schemas, 1 GraphQL BFF. Plus 3 services we know exist but have no contracts for — black boxes. Teams are polite in workshops. Contracts are honest."
</Notes>

---

## What do we have?

<CompareTable headers="Service|Endpoints|Schemas|Events|Version|Last Modified|Confidence" rows="Shipment|7|10|6|v2|2 weeks ago|High||Consignee|6|9|NONE|v1|3 weeks ago|High||Inventory|7|8|—|v1|2 months ago|Medium||Carrier|7|10|7|v1|1 week ago|High||Invoicing|8|11|NONE|v1|6 months ago|Low||Tracking|4|8|—|v1|14 months ago|Very Low" />

<Notes>
"Quick inventory of the artifacts. 6 services. Most have schemas, most have endpoints. But look at the events column — Consignee says NONE explicitly. Tracking and Inventory have nothing. Look at versioning — Shipment is on v2 already, someone's invested there. And look at last-modified — Invoicing 6 months, Tracking 14 months. The confidence column is how much we trust each contract: high where teams actively maintain them, very low where they don't. This is the starting evidence."
</Notes>

---

## Vocabulary — The Person Concept

> *Who receives the delivery?*

<Split ratio="3:1">
<div>

<CompareTable colWeights="1|1|1|1|3" headers="Service|Term Used|ID Field|Fields|Infer" rows="Shipment|buyer|buyerId|3|the person placing the order?||Consignee|customer|customerId|20|the full customer profile?||Inventory|user|userId|1|who requested the reservation?||Carrier|recipient|recipientId|3|who receives the package?||Invoicing|account|accountId|3|billing entity — person or company?||Tracking|user|userId|2|who gets notified?" />

</div>
<div>

<Callout type="warning">
**🚩 Validate with domain experts**

**Consignee has 20 fields** for this concept (segments, loyalty, preferences, addresses). Everyone else has 3.

→ Who is the **system of record**?

→ Is "account" **intentionally different** (billing entity vs person)?

→ 5 names, same fields — **drift or design**?
</Callout>

</div>
</Split>

<Notes>
"Five names for what looks like the same concept. Same three fields — ID, name, email. But 'account' in Invoicing might mean the billing entity, not the person — that could be intentional. We flag it, we don't judge it. What we CAN say: Consignee has 20 fields. Everyone else has 3. Five thin copies of a concept one service owns richly. Who is the system of record? That's the question we take to the domain experts."
</Notes>

---

## Vocabulary — The Address Concept

> *Where do they deliver?*

<Split ratio="3:1">
<div>

<CompareTable colWeights="1|2|3|1|2" headers="Service|Schema Name|Unique Fields|Fields|Infer" rows="Shipment|Address|deliveryInstructions, taxId|11|mixed concerns||Consignee|CustomerAddress|label, isDefault|10|customer-managed||Carrier|DeliveryAddress|accessCode, isResidential|11|operational||Invoicing|BillingAddress|vatNumber, companyName|10|legal document" />

</div>
<div>

<Callout type="tip">
**✅ Correct DDD**

Different names **AND** different fields. Each context adds fields for **ITS purpose**:

**Carrier** needs `accessCode` → for the delivery driver

**Invoicing** needs `vatNumber` → for the legal document

**Consignee** needs `label` → "Home" / "Work"

Each context **SHOULD own its own Address** representation.
</Callout>

</div>
</Split>

<Notes>
"Now compare with Address. Same four services. But look — different names AND different fields. Carrier needs access codes for the delivery driver. Invoicing needs VAT numbers for the legal document. Consignee needs labels like Home and Work. They look the same but they serve different purposes. This IS correct DDD — each bounded context should own its own Address. The contracts just proved it."
</Notes>

---

## The God Entity

> *Shipment Service · openapi.yaml · components/schemas/Order*

<Split ratio="2:1">
<div>

<CodeBlock title="shipment-service · openapi.yaml" code="components:&#10;  schemas:&#10;    Order:&#10;      type: object&#10;      properties:&#10;        orderId: string&#10;        status: OrderStatus&#10;        lines: array&#10;        totalAmount: number&#10;        createdAt: date-time&#10;&#10;        # from CONSIGNEE&#10;        buyerId: string&#10;        buyerName: string&#10;        buyerEmail: string&#10;&#10;        # from INVENTORY&#10;        warehouseId: string&#10;        warehouseName: string&#10;&#10;        # from CARRIER&#10;        carrierId: string&#10;        carrierName: string&#10;        trackingNumber: string&#10;&#10;        # from INVOICING&#10;        invoiceId: string&#10;        invoiceNumber: string&#10;        paymentMethod: string&#10;        paymentStatus: string" />

</div>
<div>

<Callout type="warning">
**⚠️ One schema. Four other contexts.**

**5 own fields. 12 borrowed from 4 contexts.**

Every change in Consignee, Inventory, Carrier, or Invoicing **potentially breaks this schema**.

This is a **Big Ball of Mud** at the boundary level.

**🚩 Validate**

→ Does Shipment **NEED** all these fields, or is it just convenient?

→ Can any be replaced with an **ID-only** reference?
</Callout>

</div>
</Split>

<Notes>
"Here's why it's a god entity. Look at the Order schema. 5 fields are its own — orderId, status, lines, total. Fine. But then — buyerId, buyerName, buyerEmail from Consignee. warehouseId, warehouseName from Inventory. carrierId, carrierName, trackingNumber from Carrier. invoiceId, invoiceNumber, paymentMethod, paymentStatus from Invoicing. 12 fields from 4 other contexts embedded in one schema. Every change in any of those contexts potentially breaks this one schema. The question for domain experts: does Shipment actually NEED all this, or did it just accumulate over time?"
</Notes>

---

## Coupling Heatmap

> *Who depends on whom?*

<Split ratio="1:1">
<div>

<HeatmapMatrix labels="Ship|Cons|Inv|Car|Inv$|Track" rowNames="Shipment|Consignee|Inventory|Carrier|Invoicing|Tracking" cells="-|x|x|x|x|.||.|-|.|.|.|.||x|x|-|.|.|.||x|x|x|-|.|.||x|x|.|.|-|.||x|x|.|x|x|-" highlightRow="Shipment" highlightCol="Cons" />

</div>
<div>

<Callout type="warning">
**🚩 Three signals**

→ **Shipment** references **ALL 4** services. One service pulling from everywhere. **God service?**

→ **Consignee** referenced by all, references nobody. Clean upstream — but **0 events**. **Sync bottleneck?**

→ **Shipment ↔ Carrier**: circular. Both reference each other. **Dead boundary?**
</Callout>

</div>
</Split>

<Notes>
"The coupling heatmap. Trace which services reference each other's concepts through ID fields and shared schemas. Three signals jump out. First — Shipment's row has 4 crosses. It pulls data from every other service. That's a god service. Second — Consignee's column has 5 crosses. Everyone references it but it references nobody. That looks clean — but it publishes zero events, so every dependency is a synchronous API call. Third — Shipment and Carrier reference each other. Circular. That's a dead boundary signal."
</Notes>

---

## Dead Boundary vs Boundary Violation

> *Not all coupling is the same*

<Split ratio="2:1">
<div>

<CodeBlock title="shipment-carrier · coupling evidence" code="# SHIPMENT SERVICE defines:&#10;  ShipmentInfo:&#10;    carrierId: string&#10;    carrierName: string&#10;    trackingNumber: string&#10;    estimatedDelivery: date&#10;&#10;# CARRIER SERVICE defines:&#10;  Shipment:&#10;    shipmentId: string&#10;    orderId: string        # from Shipment&#10;    recipientId: string    # from Consignee&#10;    carrierName: string&#10;    trackingNumber: string&#10;&#10;# Same concept. Two owners.&#10;# Circular references." />

</div>
<div>

<Callout type="danger" label="DEAD BOUNDARY">
Both services define **Shipment** schema. Both reference each other. **Circular.**

**Duplicate ownership** of the same concept.

→ **MERGE** into one context or build a proper **ACL**.
</Callout>

</div>
</Split>

<Split ratio="2:1">
<div>

<CodeBlock title="shipment-inventory · coupling evidence" code="# SHIPMENT SERVICE references:&#10;  Order:&#10;    warehouseId: string    # from Inventory&#10;&#10;# INVENTORY SERVICE references:&#10;  Reservation:&#10;    orderId: string        # from Shipment&#10;&#10;# Different concepts. Shared IDs only.&#10;# No schema duplication." />

</div>
<div>

<Callout type="warning" label="BOUNDARY VIOLATION">
Share IDs only. **No duplicate schemas.**

**Order ≠ Reservation.** Different concepts.

The **integration pattern is wrong**, not the boundary.

→ **FIX the pattern** (replace co-write with saga/events)
</Callout>

</div>
</Split>

<Notes>
"Not all coupling is the same. Look at the evidence side by side. Shipment and Carrier both define a Shipment schema — carrierId, trackingNumber, same fields, two owners. Circular references. That's a dead boundary — same concept, two services pretending to be separate. Either merge or build an ACL. Now Shipment and Inventory — they share IDs only. orderId and warehouseId. But Order is not the same concept as Reservation. These are genuinely different domains coupled by the wrong integration pattern. Don't merge — fix the pattern. Replace the shared transaction with events or a saga."
</Notes>

---

## The model updates

<ContextMap version="1" />

> Contracts can lie. Let's check the database.

<Notes>
"Based on what the contracts declared, here's our first hypothesis. Same circles. Now with evidence. Shipment lit up amber as our god entity. Coupling lines outward — buyerId to Consignee, warehouseId to Inventory, invoiceId to Invoicing. The Shipment-Carrier line is dotted — dead boundary suspected. Consignee labelled '0 events' — sync bottleneck. The three black boxes still unknown. Hypothesis v0.1 — what the contracts declared. But contracts can lie. The database is next."
</Notes>

---

<TitleCard badge="EXHIBIT B" subtitle="The API says 'I own this.' The database says 'so does he.'">Schema Archaeology</TitleCard>

<Notes>
"Second lens. The contracts showed us what teams declared. Now let's check if those declarations are real. The database doesn't have opinions. It just records who accessed what."
</Notes>

---

## The Shared Table Map

> *Which tables do multiple services access?*

<Split ratio="3:1">
<div>

<CompareTable colWeights="2|2|3|1" headers="Table|Writers|Readers|Severity" rows="orders|Shipment|Carrier · Invoicing · Reporting|🟠 HIGH||inventory_reserved|Shipment · Inventory|—|🔴 CRITICAL||customer_addresses|Consignee|Shipment · Carrier · Invoicing|🟠 HIGH||shipment_status|Carrier|Shipment|🟡 MEDIUM||payment_ledger|Invoicing|Reporting|🟢 CLEAN" />

</div>
<div>

<Callout type="warning" label="KEY DISTINCTION">
**Read coupling = dependency**

Service A needs Service B's data. **Manageable.**

**Write coupling = boundary violation**

Two services believe they own the same data. This is where **3 AM pages** come from.

→ `inventory_reserved` has **2 WRITERS**. Two services think they own reservation logic.
</Callout>

</div>
</Split>

<Notes>
"Five shared tables. The critical distinction: reading someone else's table is a dependency — manageable. Two services WRITING to the same table is a boundary violation — two aggregate owners. inventory_reserved has two writers — Shipment and Inventory both write to it. Two services believe they own reservation logic. That's where your race conditions come from. And look at payment_ledger — one writer, one reader. That's clean. That's how it should work."
</Notes>

---

## The Facade Reveal

<ContradictionReveal
  earlierExhibit="Exhibit A"
  earlierClaim="Consignee Service: clean boundary ✓ | Referenced by all. References nobody. | Published language."
  laterExhibit="Exhibit B"
  laterFinding="Consignee's customer_addresses table | Read directly by 3 services | Shipment, Carrier, Invoicing — all bypass the API | The boundary is a facade ✗"
  connector="but the database says…"
  punchline="Contracts declared a boundary. The database showed it doesn't exist."
/>

<Notes>
"Remember Exhibit A? Consignee looked clean — referenced by everyone, references nobody. A textbook published language. Now look at the database. Three services — Shipment, Carrier, Invoicing — read the customer_addresses table directly. They bypass the Consignee API entirely. The API boundary is a facade. The database tells a different story. This is why you need multiple lenses — one artifact alone can mislead you."
</Notes>

---

## Ghost Users

> *Two database accounts with no corresponding running service*

<Split ratio="2:1">
<div>

<CodeBlock title="database-users · access audit" code="# Active service users:&#10;  svc_shipment     → orders, order_lines       (READ/WRITE)&#10;  svc_consignee    → customers, addresses     (READ/WRITE)&#10;  svc_inventory    → stock_levels, reservations (READ/WRITE)&#10;  svc_carrier      → shipments, tracking      (READ/WRITE)&#10;  svc_invoicing    → invoices, payments       (READ/WRITE)&#10;&#10;# Unknown users:&#10;  svc_etl_legacy   → orders                   (READ)   # ??? no known service&#10;  svc_vendor_sync  → orders                   (READ)   # ??? no known service" />

</div>
<div>

<Callout type="warning" label="GHOST USERS">
**⚠️ Two database users with no corresponding running service.**

Decommissioned processes? ETL pipelines nobody documented? Vendor tools "turned off" but **credentials still work**?

Both are **read-only** — manageable.

A ghost user with **WRITE** access would be the most dangerous finding of all.
</Callout>

</div>
</Split>

<Notes>
"One more thing the database shows — ghost users. Two database accounts with no corresponding running service. svc_etl_legacy and svc_vendor_sync. Both read-only against the orders table. Probably decommissioned processes whose scheduled jobs still run, or vendor tools that were turned off but the credentials still work. Both are read-only, so manageable. But imagine a ghost user with WRITE access — a process nobody knows about, modifying data in your production database. The database sees everything, including the ghosts."
</Notes>

---

## The model updates

<ContextMap version="2" />

> *Transactions next. What commits together belongs together.*

<Notes>
"Update the model. Inventory turns red — two writers, boundary violation. Consignee gains the 'facade' finding — clean API but bypassed by the database. The dashed red line between Shipment and Inventory marks the shared-write coupling. The three black boxes still unknown. Hypothesis v0.2 — what the database reveals beneath. Transactions next."
</Notes>

---

<TitleCard badge="EXHIBIT C" subtitle="What commits together, belongs together.">Transaction Boundaries</TitleCard>

<Notes>
"Third lens. The contracts showed what teams declared. The database showed real coupling. Now we go one layer deeper — what the database COMMITS TOGETHER. Because every BEGIN...COMMIT block is a consistency boundary. In DDD terms — that's an aggregate. Whether anyone designed it or not."
</Notes>

---

## Transaction Clusters

> *Which tables commit together?*

<Split ratio="3:1">
<div>

<CompareTable colWeights="3|1|1|2" headers="Tables in same transaction|Frequency|Service|Verdict" rows="{orders · order_lines · order_audit}|84,219/wk|Shipment|✅ Order Aggregate||{shipments · tracking_events}|31,445/wk|Carrier|✅ Shipment Aggregate||{payment_ledger · payment_events}|9,877/wk|Invoicing|✅ Payment Aggregate||{orders · shipments}|2,103/wk|Shipment|⚠️ CROSS-CONTEXT||{orders · inventory_reserved}|4,512/wk|Shipment|⚠️ BOUNDARY VIOLATION" />

</div>
<div>

<Callout type="warning" label="TWO WARNINGS">
**`{orders · shipments}` — 2,103/wk**

**300× per day.** Not noise. Shipment auto-creates a carrier record in the same commit.

→ **Dead boundary confirmed** at the transaction level.

**`{orders · inventory_reserved}` — 4,512/wk**

Average duration: **580ms.** Holding locks on 2 tables for over half a second.

→ Boundary violation **AND** a performance problem.
</Callout>

</div>
</Split>

<Notes>
"We mined 7 days of transactions. Clean aggregates emerge immediately — orders with order_lines, 84,000 times a week. That's the Order aggregate. Solid. Shipments with tracking_events — the Shipment aggregate in the Carrier context. Clean. But then — orders and shipments committed together 2,103 times per week. 300 times a day. The Shipment Service writes to Carrier's table in the same commit. That's the dead boundary from Exhibit A, confirmed at the transaction level. You cannot split these services without breaking this transaction. And orders with inventory_reserved — 4,512 times at 580 milliseconds average. That's a distributed concern handled locally. Works in a monolith. Breaks the moment you try to extract."
</Notes>

---

## Extraction Readiness

> *Can we extract these services?*

<Split ratio="2:1">
<div>

<ContentCard title="Carrier Integration · 🟢 READY" accent="green" items="Clean internal transactions, No cross-context co-writes" />

<ContentCard title="Consignee · 🟢 READY" accent="green" items="Clean internal transactions, No cross-context co-writes" />

<ContentCard title="Shipment Service · 🔴 BLOCKED" accent="red" items="Co-writes to Carrier: 2103/wk, Co-writes to Inventory: 4512/wk" />

<ContentCard title="Inventory · 🔴 BLOCKED" accent="red" items="Shared writes with Shipment" />

</div>
<div>

<Callout type="warning" label="THE RULE">
**High-frequency co-writes WITHIN a service** = aggregate. The system designed it.

**Cross-service co-writes** = boundary violation. Blocks extraction.

→ **Fix the co-write FIRST** (saga, events), **THEN** extract.

**⚠️ But Exhibit H will override one of these green lights…**
</Callout>

</div>
</Split>

<Notes>
"From the transaction data we can already assess extraction readiness. Carrier Integration — clean, all transactions internal, ready to extract. Consignee — same, clean and ready. Shipment — blocked. It co-writes to Carrier's table 2,103 times a week and to Inventory's table 4,512 times. You cannot extract Shipment without breaking those transactions. Inventory — blocked for the same reason. The rule is simple: high-frequency co-writes within a service is a designed aggregate. Cross-service co-writes block extraction. Fix the co-write first — replace it with a saga or events — then extract. But keep an eye on those green lights — Exhibit H has something to say about one of them."
</Notes>

---

## The Timestamp Connection

> *Hidden domain events the system tracks but never publishes*

<Split ratio="2:1">
<div>

<CodeBlock title="orders · table schema (from Exhibit B)" code="created_at:    timestamp    # implicit: OrderCreated&#10;confirmed_at:  timestamp    # implicit: OrderConfirmed&#10;shipped_at:    timestamp    # implicit: OrderShipped&#10;delivered_at:  timestamp    # implicit: OrderDelivered&#10;cancelled_at:  timestamp    # implicit: OrderCancelled&#10;&#10;# Each timestamp is set WITHIN a transaction cluster.&#10;# The transaction that sets confirmed_at&#10;# IS the OrderConfirmed event." />

</div>
<div>

<Callout type="tip" label="HIDDEN DOMAIN EVENTS">
**Every lifecycle timestamp** = an implicit domain event that nobody publishes.

The transaction cluster tells you:

→ **WHAT** changed (which tables)

→ **WHEN** it changed (the timestamp)

→ **TOGETHER** with what (the aggregate)

That's the **event payload**.

These are the events the system **should be publishing but isn't**.

🔗 **Exhibit D** will show us these events fossilized in the logs.
</Callout>

</div>
</Split>

<Notes>
"One more connection before we move on. Remember the lifecycle timestamps from Exhibit B — confirmed_at, shipped_at, delivered_at? Each of those timestamps is set INSIDE a transaction cluster. The transaction that sets confirmed_at on the order IS the OrderConfirmed event. The tables modified in that transaction ARE the event payload. The system has been tracking these state changes for years — it just never announced them. These are the domain events the system should be publishing. Exhibit D will show us these exact events, fossilized in the production logs."
</Notes>

---

## The model updates

<ContextMap version="3" />

> *Runtime next. What actually happens when an order flows through?*

<Notes>
"Update the model. Carrier turns green — clean internal transactions, ready to extract. Shipment picks up the BLOCKED label — cross-context co-writes block extraction. The Shipment-Inventory line now carries the perf cost: 4,512 per week at 580 milliseconds. The three black boxes still unknown. Hypothesis v0.3 — what commits together. What blocks extraction. Runtime next."
</Notes>

---

<TitleCard badge="EXHIBIT D" subtitle="Every logger.info is a fossilized domain event — published to nobody.">Log Mining</TitleCard>

<Notes>
"Fourth lens. We've read structure — contracts, database, transactions. All static artifacts. Now we go live. Production logs. The system narrating itself in real time. Remember the language analogy? The contracts are the written signs. The logs are the SPOKEN language — the system telling you what it actually does."
</Notes>

---

## The Flow Trace

> *Trace: order ORD-2024-881742*

<CodeBlock title="production · log trace · order lifecycle" code="# SYNC chain (2 seconds) ↓&#10;09:14:02  Shipment        Order CREATED (DRAFT)&#10;09:14:02  Shipment        Inventory check requested&#10;09:14:03  Inventory       Reserve: SKU-1194, qty=3&#10;09:14:03  Shipment        Order CONFIRMED&#10;09:14:04  Invoicing       Invoice INV-39201 generated&#10;09:14:04  Tracking        Email sent: order_confirmed&#10;&#10;── 87 second gap ──&#10;&#10;# ASYNC boundary ↓&#10;09:15:31  Carrier         Shipment SHP-77012 created&#10;&#10;── 5 hours ──&#10;&#10;14:22:08  Carrier         Picked up by DHL&#10;&#10;── next day ──&#10;&#10;08:41:19  Carrier         Delivered, signed=J.SMITH&#10;08:41:20  Shipment        Order status=DELIVERED&#10;08:41:20  Tracking        Email sent: order_delivered" />

<Notes>
"We traced one order across all services. Sorted chronologically. Look at the timing. 09:14:02 to 09:14:04 — creation through invoice generation — 2 seconds. That's synchronous. Shipment calls Inventory, waits for the response, confirms the order, calls Invoicing, waits, then Tracking fires. A chain. Then — 87 second gap before Carrier creates the shipment. That's asynchronous. Carrier is already decoupled at runtime. The timing reveals the architecture: synchronous chain from creation through invoicing, asynchronous to carrier."
</Notes>

---

## Sync vs Async Boundary

<Split ratio="1:1">
<div>

<Callout type="danger" label="SYNC CHAIN · 2 seconds">
**🔴 Shipment → Inventory → Shipment → Invoicing → Tracking**

Each step **WAITS** for the previous.

If Inventory is slow, **EVERYTHING** is slow. If Invoicing is down, orders **STOP**.

This chain is the **extraction blocker**. Break any link = break the flow.

→ Confirms **Exhibit C**: `{orders, inventory_reserved}` co-write. Same coupling, different evidence.
</Callout>

</div>
<div>

<Callout type="tip" label="ASYNC BOUNDARY · 87 seconds">
**🟢 Invoicing ···87s gap··· Carrier**

Carrier picks up from a queue or poll.

If Carrier is down, orders still get created.

This boundary **ALREADY EXISTS**. Carrier is already decoupled at runtime — even though Exhibit A showed API coupling.

→ This is your **easiest extraction point**.
</Callout>

</div>
</Split>

<Notes>
"Two architectural patterns in one trace. On the left — a synchronous chain. Shipment calls Inventory, waits, confirms, calls Invoicing, waits. Two seconds, four services, all blocking. If any service in that chain is slow, the whole order creation is slow. This confirms Exhibit C's transaction finding — the same coupling, different evidence. On the right — an 87-second gap before Carrier. That's async. Carrier is already decoupled at runtime. Even though Exhibit A showed API coupling between Shipment and Carrier, at runtime they're already separated by a queue. That's your easiest extraction point — the boundary already exists in practice."
</Notes>

---

## The Event Storming Comparison

<ContradictionReveal
  earlierExhibit="Event Storming (2 years ago)"
  earlierClaim="Payment → Inventory → Shipment"
  laterExhibit="Production (today)"
  laterFinding="Inventory → Confirmation → Invoice | Payment doesn't happen at order time at all. | The sequence is different. | The participants are different."
  connector="but production says…"
  punchline="The gap between what people remember and what the system does — that's the most dangerous kind of technical debt. | It's not in the code. It's in the team's understanding."
/>

<Notes>
SLOW DOWN. Pause before reading the comparison. Let the audience read it themselves first. Then: "The event storming from two years ago said: Payment, then Inventory, then Shipment. Production says: Inventory reserved FIRST, then order confirmed, then invoice generated. Payment doesn't happen at order time at all. The sequence is different. The participants are different. [PAUSE 3 SECONDS] The team's mental model was wrong. Not because they're bad at their jobs. Because the system evolved and the documentation didn't. The gap between what people remember and what the system does — that's the most dangerous kind of technical debt. It's not in the code. It's in the team's understanding."
</Notes>

---

## The Silent Participant

> *Who else is in the flow?*

<Split ratio="3:2">
<div>

<CodeBlock title="trace · tracking appearances" code="09:14:04  Tracking   Email sent: order_confirmed&#10;08:41:20  Tracking   Email sent: order_delivered" />

**Tracking Notifications appears TWICE.**

Nobody mentioned it in Event Storming. Nobody put it on the context map.

It owns no state transitions. It makes no domain decisions. But it participates in **every order lifecycle event**.

</div>
<div>

<Callout type="warning" label="SILENT PARTICIPANT">
**🚩 Tracking is triggered by status transitions it observes.**

If CONFIRMED or DELIVERED transitions change — **Tracking breaks silently**.

Confirms **Exhibit A**: coupled to everything, owns nothing.

→ **Infrastructure pretending to be a domain service.**
</Callout>

</div>
</Split>

<Notes>
"One more finding from the logs. Tracking Notifications appears twice — at confirmation and at delivery. Nobody mentioned it in the event storming. Nobody put it on the context map. But it's silently participating in every order lifecycle transition. It owns no state. It makes no decisions. But if the confirmation or delivery transitions change, Tracking breaks — silently, because it's just sending emails. This confirms what Exhibit A suggested — Tracking is infrastructure pretending to be a domain service."
</Notes>

---

## Fossilized Event Catalog

> *Events the system publishes to nobody*

<Split ratio="3:1">
<div>

<CompareTable colWeights="2|1|1|1" headers="Fossilized Event|Daily Count|Service|Category" rows="Order CREATED|12,034|Shipment|🟢 core||Inventory RESERVED|11,891|Inventory|🟢 core||Order CONFIRMED|11,756|Shipment|🟢 core||Invoice GENERATED|11,402|Invoicing|🟢 core||Shipment CREATED|10,877|Carrier|🟢 core||Shipment DELIVERED|9,234|Carrier|🟢 core||Order CANCELLED|278|Shipment|🟡 edge case||Refund PROCESSED|156|Invoicing|🟡 edge case||Shipment FAILED|89|Carrier|🔴 error" />

</div>
<div>

<Callout type="tip" label="THESE ARE YOUR EVENTS">
**💡 Every log line with a state transition** = a domain event the system never formalized.

The **happy path**: ~11,000/day.

The edge cases nobody mentioned in Event Storming:

→ **278** cancellations/day

→ **156** refunds/day

→ **89** delivery failures/day

🔗 **Remember Exhibit C:** The timestamps (`confirmed_at`, `shipped_at`) are set in the same transactions as these log events. **Same events. Two independent sources.**
</Callout>

</div>
</Split>

<Notes>
"When you trace 1,000 orders instead of one, you get the full event catalog. The system publishes these events to nobody — they exist only in log files. The happy path processes about 11,000 orders per day. But look at the bottom — 278 cancellations, 156 refunds, 89 delivery failures. Every day. Event storming rarely covers these edge cases. The logs show you every path the system actually takes, including the ones nobody remembers designing. And remember Exhibit C — the lifecycle timestamps? confirmed_at is set in the same transaction as this 'Order CONFIRMED' log event. Two independent sources showing the same events. The evidence is converging."
</Notes>

---

## The model updates

<ContextMap version="4" />

> *Incidents next. What does it cost when boundaries are wrong?*

<Notes>
"Update the model. Tracking's finding evolves from 'infra?' to 'silent participant' — observed in every order lifecycle, owns nothing. The sync chain (Shipment → Inventory → Invoicing) gets marked red. The async path (Shipment → Carrier, 87s gap) gets marked green — already decoupled. Plus we've named a whole catalogue of fossilized events the system publishes to nobody. The three black boxes still unknown. Hypothesis v0.4 — what actually happens at runtime. The Event Storming is contradicted. Incidents next."
</Notes>

---

<TitleCard badge="EXHIBIT E" subtitle="The architecture is the bug.">Incident Clustering</TitleCard>

<Notes>
"Fifth lens. We've mapped structure — contracts, database, transactions. We've seen behavior — the logs. Now the question every engineering manager asks: so what? What does this COST us? Let's look at where the system breaks."
</Notes>

---

## The Headline Number

<BigStat value="83" label="incidents in 12 months" />

<BigStat value="64" label="at service boundaries" />

<BigStat value="77%" tone="amber" label="of all failures are cross-boundary" punchline="The system isn't failing because services are buggy. It's failing because the boundaries are wrong." />

<Notes>
"83 production incidents in 12 months. 64 of them — 77% — at service boundaries. One incident roughly every 4 days. 10 of them SEV1 — 10 nights someone got paged at 3 AM because of a boundary problem. The system is not failing because individual services are buggy. It's failing because the boundaries between them are in the wrong places."
</Notes>

---

## Boundary Incident Map

> *Where do failures cluster?*

<Split ratio="3:1">
<div>

<RankedBars bars="Shipment ↔ Inventory::23::red|Within single service::19::green|Shipment ↔ Carrier::17::amber|Invoicing ↔ Shipment::14::amber|Carrier ↔ Tracking::6::yellow|Inventory ↔ Warehouse::4::yellow" />

</div>
<div>

<Callout type="warning" label="READ THE PATTERN">
**Incidents don't cluster randomly.**

**23** at Shipment ↔ Inventory = not bad luck. = **structural coupling**.

**0** at Consignee boundary = **confirmed clean**.

Internal incidents: only **1 SEV1**. Boundary incidents: **10 SEV1s**.

→ The **severity** skews to boundaries, not just the count.
</Callout>

</div>
</Split>

<Notes>
"The boundary incident map. 23 incidents at Shipment-Inventory — the most. 17 at Shipment-Carrier. 14 at Invoicing-Shipment. And zero — zero — at the Consignee boundary. Look at the severity. 10 out of 11 SEV1s are cross-boundary. The most damaging failures are overwhelmingly at boundaries. And 19 internal incidents — only 1 SEV1. The boundaries are where the pain is."
</Notes>

---

## The Top Cluster Decomposed

> *Shipment ↔ Inventory: 23 incidents*

<Split ratio="3:2">
<div>

<PieChart slices="Race condition::30::highlight|Timeout::26|Stale read::22|Orphaned reservation::22" annotations="Exhibit B: 2 writers to inventory_reserved (7 races)|Exhibit C: 580ms co-write under load (6 timeouts)|Read replica lag (5 stale reads)|Exhibit C: missing saga (5 orphans)" />

</div>
<div>

<Callout type="danger" label="EVERY INCIDENT TRACES BACK">
The **2-writer violation** (Exhibit B) → **7 race conditions**

The **580ms co-write** (Exhibit C) → **6 timeouts**

The **missing saga** (Exhibit C) → **5 orphaned reservations**

The coupling isn't theoretical. It has a cost:

**4 SEV1s. 4 nights someone got paged. 4 war rooms.**

The evidence is converging.
</Callout>

</div>
</Split>

<Notes>
"Let me decompose the top cluster. 23 incidents at Shipment-Inventory. 7 race conditions — both services write to inventory_reserved. That's Exhibit B's two-writer violation, now costing us real pages. 6 timeouts — the 580-millisecond synchronous co-write from Exhibit C, under load it times out. 5 orphaned reservations — order cancelled but inventory not released — the missing saga that Exhibit C said we need. Every incident traces back to a finding from a previous exhibit. The coupling we found in artifacts now has a measurable cost. 4 SEV1s. 4 nights someone got paged at 3 AM because of a boundary problem."
</Notes>

---

## What's Not Clustering

> *Urgent vs important*

<Split ratio="1:1">
<div>

<Callout type="danger" label="WHERE IT BREAKS">
**Shipment ↔ Inventory** — 23 incidents

**Shipment ↔ Carrier** — 17 incidents

**Invoicing ↔ Shipment** — 14 incidents

These boundaries are in the wrong place or using the wrong integration pattern.

→ **Fix these FIRST.**
</Callout>

</div>
<div>

<Callout type="tip" label="WHERE IT DOESN'T">
**Consignee boundary** — 0 incidents

The boundary IS clean. The data access isn't (**Exhibit B**: 3 direct readers).

But **zero incidents** means the coupling hasn't HURT yet.

→ **Fix later.** Not urgent.

This is a **ticking clock**, not a fire.
</Callout>

</div>
</Split>

<Notes>
"But not everything is broken. Consignee boundary — zero incidents. Zero. The boundary IS clean — even though Exhibit B showed 3 services reading its table directly. The coupling exists but it hasn't hurt yet. That's the difference between urgent and important. The left side — fix now, people are getting paged. The right side — fix later, it's a ticking clock, not a fire. Incident clustering tells you what hurts NOW. The other exhibits tell you what will hurt NEXT."
</Notes>

---

## The model updates

<ContextMap version="5" />

> *Data lineage next. Who owns the truth when there are four copies of the address?*

<Notes>
"Update the model. The Shipment-Inventory line gets thicker — 23 incidents, the worst cluster. Shipment-Carrier thicker too — 17 incidents. Consignee turns green — confirmed clean by the incident data, zero failures. The three black boxes still unknown. Hypothesis v0.5 — what it costs when boundaries are wrong. The pattern is now quantitative. Data lineage next."
</Notes>

---

<TitleCard badge="EXHIBIT F" subtitle="Each copy with different rules is a bounded context boundary.">Data Lineage</TitleCard>

<Notes>
"Sixth lens. We've mapped structure, behavior, cost. Now we trace one specific piece of data — the delivery address — through every place it's stored. Because in a legacy system, data doesn't live in one place. It lives in four. Maybe six."
</Notes>

---

## The Copy Chain

> *Who owns the truth when there are four copies?*

<LineageTree
  source="CONSIGNEE::source of truth · customer-managed"
  branches="SHIPMENT::📸::Snapshot::ORDER CONTEXT::Frozen at order time·Never refreshed|CARRIER::🔄::Mutable::DELIVERY CONTEXT::Updated by dispatch independently·Without telling source|INVOICING::📄::Lossy::FINANCIAL CONTEXT::Concatenated string·Can't parse fields back|WAREHOUSE::📚::Archive::ANALYTICS CONTEXT::Full history (SCD Type 2)·Better data than source"
  footer="342 mismatches in 90 days"
/>

<Notes>
"We traced the delivery address from source to every copy. Consignee is the source of truth — customer-managed. Four copies. Shipment snapshots it at order time — never updates it. Carrier can update it independently if the customer calls dispatch — without telling anyone. Invoicing stores it as one concatenated string — lossy, can't be parsed back into fields. And the data warehouse — SCD Type 2 — actually has BETTER history than the source, because Consignee only stores current. 342 mismatches in 90 days between the copies."
</Notes>

---

## The Mismatch

> *Three answers. Same order. None wrong in their context.*

<Split ratio="1:1">
<div>

<CodeBlock title="support · ORD-2024-881742" code="CUSTOMER:  Where is my order going?&#10;&#10;# Support checks Shipment Service&#10;  → 123 Old Street&#10;    (snapshot from order time)&#10;&#10;# Support checks Carrier Integration&#10;  → 456 New Street&#10;    (customer called dispatch)&#10;&#10;# Support checks Invoicing&#10;  → 'John Smith, 123 Old St, Amsterdam...'&#10;    (can't even parse the fields)&#10;&#10;# THREE ANSWERS. SAME ORDER.&#10;# None wrong in their context." />

</div>
<div>

<Callout type="warning" label="THE REAL PROBLEM">
**The copies aren't bugs.** Each has a legitimate purpose:

→ **Shipment** = "address at order time"

→ **Carrier** = "where to deliver today"

→ **Invoicing** = "legal billing record"

**The problem is NO PROPAGATION.**

When the customer changes address:

→ Carrier updates ✓

→ Shipment doesn't know ✗

→ Consignee doesn't know ✗

→ Invoice can't be updated ✗

**Missing event:** `DeliveryAddressChanged`
</Callout>

</div>
</Split>

<Notes>
"When a customer calls support and asks 'where is my order going?' — the answer depends on which service the agent looks at. Shipment says the old address — snapshot from order time. Carrier says the new address — the customer called dispatch and changed it. Invoicing has it as one concatenated string you can't even parse. Three answers, same order, none wrong in their own context. The copies aren't bugs. Each serves a legitimate purpose. The problem is there's no propagation. When the address changes, nobody tells the others. The missing domain event — DeliveryAddressChanged — should exist but doesn't."
</Notes>

---

## Each Copy = A Boundary

> *Different format. Different update policy. Different purpose.*

<LineageTree
  branches="SHIPMENT::📸::Snapshot::ORDER CONTEXT::Frozen at order time·Never refreshed|CARRIER::🔄::Mutable::DELIVERY CONTEXT::Updated by dispatch independently·Without telling the source|INVOICING::📄::Lossy::FINANCIAL CONTEXT::Concatenated · immutable·Can't parse fields back|WAREHOUSE::📚::Archive::ANALYTICS CONTEXT::SCD Type 2 · full history·Better data than source"
  footer="Each copy with different rules IS a bounded context"
/>

<Notes>
"Four copies. Four completely different characters. Shipment freezes the address — a snapshot, never updated. Carrier keeps it mutable — operational, changes before dispatch. Invoicing concatenates it into a legal document — lossy, irreversible. The warehouse versions it with full history — accidentally has better data than the source. Different format. Different update policy. Different purpose. Each copy with different rules IS a bounded context boundary. The fix isn't centralizing the data. It's acknowledging these boundaries exist and building the propagation events between them."
</Notes>

---

## The model updates

<ContextMap version="6" />

> *Error codes next. What business rules has the system been quietly enforcing?*

<Notes>
"Update the model. Dotted blue lines now show data flow — the address copies radiating from Consignee outward. 342 mismatches labelled on the Consignee-Carrier line. A missing event flag — DeliveryAddressChanged — hovers between them. The three black boxes still unknown. Hypothesis v0.6 — where data drifts and events are missing. Error codes next."
</Notes>

---

<TitleCard badge="EXHIBIT G" subtitle="The errors aren't bugs — they're the system protecting itself.">Error Code Reverse-Engineering</TitleCard>

<Notes>
"Seventh lens. Every legacy system has cryptic error codes that nobody fully understands but everyone works around. These codes are fossilized business rules. Each one is a domain invariant someone decided was important enough to validate and reject. The error code table is the most honest business rules document in the entire system."
</Notes>

---

## The Decoded Rules

> *Two error codes. Two undocumented business rules.*

<Callout type="warning" label="ORD-E003 · 4,102/year 🟠">
**"Price variance exceeded"**

If price drifts more than **2%** between cart and checkout → reject the order.

That **2% exists NOWHERE in documentation. Only in code.**

Fires **11 times a day.** Does your pricing manager know it exists?
</Callout>

<Callout type="warning" label="ORD-E031 · 2,156/year 🟠">
**"Split shipment threshold"**

Items from **3+ warehouses** → triggers split-shipment logic.

This error references **warehouses, carriers, AND orders** — an invariant validating across **3 contexts** = orchestration logic that nobody gave a name to.
</Callout>

<Notes>
"Let me show you what the error codes decode to. ORD-E003 — price variance exceeded. If the price changes by more than 2% between cart and checkout, reject. That 2% threshold exists nowhere in documentation. Only in code. It fires 11 times a day. Is it still the right number? Does your pricing manager know it exists? ORD-E031 — split shipment. Items from 3 or more warehouses triggers split logic. This error code references warehouses, carriers, AND orders — it validates across 3 contexts. That's orchestration logic. It needs its own context. Nobody named it."
</Notes>

---

## The Misplaced Rule + The Escape Hatch

<Split ratio="1:1">
<div>

<Callout type="warning" label="DEL-E011 · 412/year 🟡">
**"Return window validation"**

→ Electronics = 30 days

→ Clothing = 14 days

→ Perishable = none

**Lives in:** Carrier Service (`DEL-` prefix)

**Should be:** Returns / Policy

This is a business **POLICY**, not delivery **LOGISTICS**.

The prefix shows where the rule **lives**. The content shows where it **belongs**.

→ Move it to a new **Returns/Policy** context.
</Callout>

</div>
<div>

<Callout type="danger" label="ORD-E099 · 891/year 🔴">
**"Legacy override applied"**

**891 times per year**, someone bypasses validation.

**What rules does it skip?**

**Nobody knows.**

`skip_validation()` has grown over years.

No documentation. No audit trail.

🚩 **Audit this FIRST.**
</Callout>

</div>
</Split>

<Notes>
"Two more findings. DEL-E011 — return window per category. Electronics get 30 days, clothing 14, perishable gets none. That's a business POLICY. But it lives in the Carrier service — prefixed DEL because the delivery team built it. The prefix shows where the rule LIVES. The content shows where it BELONGS. This error code just discovered a missing context — Returns/Policy. Nobody designed it. The error code knew it existed. And ORD-E099 — the dangerous one. Legacy override. 891 times a year, someone on the ops team bypasses standard validation. What rules does it skip? Nobody knows completely. The function has grown over years. No documentation. No audit trail. 891 unlogged validation bypasses per year. Audit this first."
</Notes>

---

## The Load-Bearing Ratio

> *ORD-E001: "Insufficient allocation"*

<BigStat value="12,847" label="error fires per year" tone="amber" />

<BigStat value="23" label="actual incidents (from Exhibit E)" tone="red" punchline="The error handling is LOAD-BEARING. Remove this validation without understanding it → 12,847 failures instead of 23." />

<Notes>
"One more connection. Remember the 23 incidents at Shipment-Inventory from Exhibit E? ORD-E001 — insufficient allocation — fires 12,847 times per year. The error handling catches 12,824 cases successfully. Only 23 escape into incidents. The error code is load-bearing. It's not a bug — it's the system protecting itself. Remove this validation without understanding it and you get 12,847 failures per year instead of 23. That ratio tells you: the error handling is more important than you think. Understand what it protects before you touch it."
</Notes>

---

## The model updates

<ContextMap version="7" />

> *Last lens. What does git history reveal about our boundaries?*

<Notes>
"Update the model. A new context reveals itself — Returns/Policy — in purple, in place of the middle black box. DEL-E011 traces from Carrier into this new context. Shipment picks up the '⚠ 891 overrides' warning. The two remaining black boxes still unknown. Hypothesis v0.7 — business rules nobody documented. One new context discovered. Change velocity next."
</Notes>

---

<TitleCard badge="EXHIBIT H" subtitle="Git records every architectural decision, whether you meant to make it or not.">Change Velocity</TitleCard>

<Notes>
"Eighth and final lens. Seven exhibits mapped structure, data, behavior, cost, and rules. One lens left — the one that records every decision every developer ever made. Git history. If files from two services change together in the same commit, those services are coupled — regardless of what the architecture diagram says."
</Notes>

---

## The Co-Change Evidence

> *Which code actually co-evolves? (6 months of git history)*

<RankedBars max="100" bars="OrderController + ShipmentService::72::red|DeliveryScheduler + InventoryChecker::68::red|PricingEngine + OrderValidator::54::amber|InvoiceGenerator + OrderEventHandler::41::yellow|CustomerAddressValidator (solo)::89::green" />

<Notes>
"Six months of git history. Which files change together across services? OrderController and ShipmentService — 72% co-change. Nearly three quarters of the time a developer touches one, they also touch the other. DeliveryScheduler and InventoryChecker — 68%. Nobody mentioned this dependency in any previous exhibit. Git found it. PricingEngine and OrderValidator — 54%. Extract pricing last, not first — it's coupled to every order change. And CustomerAddressValidator — changes alone 89% of the time. The Consignee context IS genuinely independent. Confirmed now by 8 exhibits."
</Notes>

---

## The Override

<ContradictionReveal
  earlierExhibit="Exhibit C"
  earlierClaim="Carrier Integration: 🟢 READY | Clean internal transactions. | Transaction boundary is clean. | Ready to extract."
  laterExhibit="Exhibit H"
  laterFinding="Carrier Integration: 🔴 NOT READY | 72% co-change with Shipment | Every PR that touches one requires changes in the other."
  connector="but git overruled…"
  punchline="The transaction boundary is clean. The development reality isn't. | Git overruled Exhibit C. | This is why you need 8 lenses — it took the eighth to catch what the third missed."
/>

<Notes>
"One override. Exhibit C gave Carrier Integration a green light — clean transactions, ready to extract. We were confident. Then we opened the git history. 72% co-change with Shipment. Every PR that touches one requires changes in the other. The transaction boundary is clean. The development reality isn't. Git overruled Exhibit C. This is why you need 8 lenses — one artifact can mislead you. It took the eighth lens to catch what the third lens missed."
</Notes>

---

## What Changes Alone = What's Truly Independent

<Split ratio="1:1">
<div>

<Callout type="danger" label="COUPLED IN GIT">
**OrderController + ShipmentService** — 72%

→ dead boundary confirmed by **6 exhibits** now

**DeliveryScheduler + InventoryChecker** — 68%

→ hidden dependency. Git found what contracts didn't.

**PricingEngine + OrderValidator** — 54%

→ extract **LAST**. Stabilize the interface before separating.
</Callout>

</div>
<div>

<Callout type="tip" label="INDEPENDENT IN GIT">
**CustomerAddressValidator** — 89% solo

→ Consignee IS independent. Confirmed: A, B, C, E, H.

**CarrierRateCalculator** — 94% solo

→ well-encapsulated, clean boundary

**NotificationRenderer** — 91% solo

→ infrastructure, correctly isolated

These are your **reference points** for what good separation looks like.
</Callout>

</div>
</Split>

<Notes>
"Two sides. On the left — coupled. OrderController and ShipmentService at 72% — the dead boundary, now confirmed by 6 independent exhibits. DeliveryScheduler and InventoryChecker at 68% — a hidden dependency that none of the previous 7 exhibits caught. Git found it. PricingEngine and OrderValidator at 54% — if you try to extract pricing first, you'll be changing two services in lockstep for every feature. Extract it last. On the right — what independence looks like. CustomerAddressValidator changes alone 89% of the time. CarrierRateCalculator 94%. NotificationRenderer 91%. These are your reference points. When you're deciding whether two things belong together, compare their co-change rate to these baselines."
</Notes>

---

## The Extraction Order

> *Start with the least coupled. End with the most entangled. Git tells you the order.*

<Callout type="tip" label="1 · EXTRACT FIRST">
**Consignee** — 89% solo 🟢

Independent. Clean. **Go.**
</Callout>

<Callout type="warning" label="2 · STABILIZE FIRST">
**Invoicing** — 41% co-change 🟡

Moderate coupling. Stabilize the event contract first, then extract.
</Callout>

<Callout type="warning" label="3 · FIX BEFORE EXTRACTING">
**Inventory** — 68% co-change 🟠

Replace co-write with **saga** first. **Then** extract.
</Callout>

<Callout type="danger" label="4 · EXTRACT LAST">
**Shipment + Carrier** — 72% co-change 🔴

**Merge first.** Then decide if the merged context needs splitting differently.
</Callout>

<Notes>
"Git gives you the extraction order. Start with Consignee — 89% solo, genuinely independent, extract tomorrow. Then Invoicing — moderate coupling, stabilize the event contract between it and Shipment, then extract. Inventory needs work first — replace the co-write with a saga, then extract. Shipment and Carrier — don't extract separately. Merge them first. Then decide if the merged context needs splitting along different lines. Start with the least coupled. End with the most entangled. Git tells you the order."
</Notes>

---

## The model updates

<ContextMap version="8" />

> *Eight lenses. One evidence-backed hypothesis. Now we reconcile.*

<Notes>
"Final update. Carrier flips from green READY (Exhibit C) to red NOT READY — 72% co-change with Shipment overrules the transaction-boundary verdict. A MERGE indicator appears between them. The three black boxes — one of them is now Returns/Policy (purple), revealed by Exhibit G. The other two remain unknown. Hypothesis v0.8 — eight lenses. One evidence-backed hypothesis. The remembered domain said 3 contexts. The encoded domain shows 7."
</Notes>

---

## The model updates — v0.8 proof (blob approach)

<BoundedContextMap />

<Notes>
Proof of concept for the alternative model visualization. Same v0.8 state — Shipment+Carrier merged, Returns/Policy revealed, 2 remaining unknowns — but rendered as overlapping bounded-context regions instead of the rectangular grid. Compare to the previous slide. If this lands, it replaces ContextMap for all 9 versions (v0.0-v0.8).
</Notes>

---

<TitleCard badge="ACT 3" subtitle="The Rosetta Stone moment">The Reconciliation</TitleCard>

<Notes>
"Eight exhibits. Eight lenses. The same couplings confirmed from independent sources. Like the Rosetta Stone — the same decree written in three scripts. When eight independent artifacts say the same thing, it's not an opinion. It's a diagnosis."
</Notes>

---

## The Remembered Domain vs The Encoded Domain

<Split ratio="1:1">
<div>

**What the team remembers: 3 contexts**

<ContentCard title="FROM EVENT STORMING" accent="amber" items="ORDER CONTEXT — everything starts here: orders + delivery + inventory + pricing, WAREHOUSE CONTEXT — stock and location, CUSTOMER CONTEXT — profile and address" />

</div>
<div>

**What the system encoded: 7 contexts**

<ContentCard title="FROM FORENSIC EVIDENCE" accent="green" items="Order — lifecycle only, Delivery — independent lifecycle (Exhibits A + C), Invoicing — own lifecycle including dunning (Exhibit D), Inventory — needs its own transactional boundary (Exhibits C + E), Returns/Policy — rules scattered across services (Exhibit G), Customer — source of truth nobody uses properly (Exhibits B + F), Warehouse — split-shipment orchestration (Exhibit G)" />

</div>
</Split>

<Notes>
"Two models. Side by side. The team remembered 3 contexts. The system encoded 7. Delivery is its own context — not part of Order. Invoicing has its own lifecycle. Returns and Policy is a missing context — its rules are scattered. Customer is a source of truth nobody actually uses through its API. And Inventory needs its own transactional boundary — the coupling proved by 23 incidents and 12,847 error code fires per year."
</Notes>

---

## Extraction Playbook

<Split ratio="1:1">
<div>

<ContentCard title="THE DECISION" accent="amber" items="Causing incidents + shared by 3+ services → EXTRACT (high priority), Causing incidents + simpler coupling → EXTRACT (medium priority), Blocking team velocity but no incidents yet → WRAP with ACL first, Clean boundary confirmed by 3+ exhibits → LEAVE ALONE" />

</div>
<div>

<ContentCard title="THE SEQUENCE" accent="green" items="1. Strangle reads — API gateway in front of shared tables, 2. Introduce events — replace synchronous co-writes, 3. Shadow traffic — run new path in parallel + compare, 4. Feature-flag cutover — 1% → 10% → 50% → 100%" />

</div>
</Split>

<Notes>
"Finding the boundaries is half the work. Extracting them safely is the other half. The decision: if it's causing incidents and shared by many services — extract. If it's blocking velocity but no incidents yet — wrap with an anti-corruption layer first. If it's clean — leave it alone. The sequence: strangle reads first, then introduce events, then shadow traffic, then feature-flag the cutover. You don't rewrite. You strangle. Incrementally. With evidence telling you exactly where to cut."
</Notes>

---

## What Forensic DDD gives Event Storming

<Split ratio="1:1">
<div>

**Without forensic evidence:**

> *"How does the system work?"*

Experts build the model **from memory**. Expensive. Incomplete. Immediately starts drifting.

</div>
<div>

**With forensic evidence:**

> *"Is this how the system works?"*

Experts **react to evidence**. Faster. Grounded. Gaps are visible, not assumed.

The system tells the domain story back to us.

</div>
</Split>

<Notes>
"Forensic DDD doesn't replace Event Storming. It changes the question. Without evidence, you ask 'how does this work?' and experts build from memory. With evidence, you ask 'is this how it works?' and experts react. People are much faster at reacting than creating. You arrive with a draft model extracted from 8 independent artifacts. The experts validate, correct, and extend it. The workshop is richer, faster, and grounded in evidence instead of memory."
</Notes>

---

## The Forensic DDD Checklist

<Split ratio="1:1">
<div>

<ContentCard title="READ THE ARTIFACTS" accent="amber" items="A — API contracts: vocabulary drift + coupling, B — Database: shared tables + boundary violations, C — Transactions: aggregate boundaries, D — Logs: actual runtime behavior, E — Incidents: cost of wrong boundaries, F — Data lineage: who owns the truth, G — Error codes: undocumented business rules, H — Git: development coupling" />

</div>
<div>

**The toolkit is open source.**

`github.com/rajnavakoti/ddd-archaeology`

8 CLI commands. Synthetic dataset. Chain-of-thought documentation.

Point it at your own contracts, schemas, and logs.

</div>
</Split>

<Notes>
"Here's the checklist. Eight exhibits. Take this to your own systems. The toolkit is open source — 8 CLI commands, a synthetic delivery platform dataset, and the full chain-of-thought documentation for each technique. Point it at your own artifacts and see what the system tells you."
</Notes>

---

<Callout>
**The domain isn't in the documentation. It isn't in people's heads. It's in the system.**

Encoded in transactions. Fossilized in logs. Buried in error codes. Revealed by the patterns of what breaks.

Teams are polite in workshops. Contracts are honest. The database doesn't have opinions. Logs can't lie. Incidents don't cluster randomly. Error codes are the most honest business rules document. And git records every architectural decision, whether you meant to make it or not.

**Legacy systems aren't just technical debt. They're dense archives of domain knowledge, if you know how to read them.**
</Callout>

<Notes>
SLOW DOWN. This is the closing. Deliver each sentence with a pause. "The domain isn't in the documentation. It isn't in people's heads. It's in the system. Encoded in transactions. Fossilized in logs. Buried in error codes. [pause] Teams are polite in workshops. Contracts are honest. The database doesn't have opinions. Logs can't lie. [pause] Legacy systems aren't just technical debt. They're dense archives of domain knowledge. If you know how to read them. [pause] Thank you."
</Notes>

---

<TitleCard badge="THANK YOU" subtitle="rajnavakoti.com · github.com/rajnavakoti/ddd-archaeology">Raj Navakoti — @rajnavakoti</TitleCard>

<Notes>
Final slide. Stay up while people clap or approach for questions. If time remains, take questions. If not: "I'll be outside — find me."
</Notes>
